function [V_best,CL,time] = glideEndurance(CL_Vec,CD,Weight,rho,area,e0,AR,hight) %CD_min, CL_vec, Wieght, alt, rho, Wing Area
%Finds the CL closes to 3*CD0 = kCl^2

    CD_min = min(CD);
    
    k = 1/(pi*e0*AR);
    
    
    
    
    
    CL = CL_Vec(i);
    V_best = sqrt(((2*Weight)/(CL*area*rho)));
    
    LD = CL/CD(i);
    
    glideangle = atan(1/(LD));

    sinkRate = V_best*sin(glideangle);

    time = hight/sinkRate;

    
end